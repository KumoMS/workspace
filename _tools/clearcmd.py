import os
clear = lambda: os.system('cls')
clear()

# Typing this into the cmd line allows me to use clear() in order to clear screen
# take note how this makes use of the import os feature
# lets me use the cls function from cmd line in the python script 